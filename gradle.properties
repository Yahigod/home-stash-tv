org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.configuration-cache=true
org.gradle.parallel=true
android.useAndroidX=true
android.nonTransitiveRClass=true

# Public release identity. The release workflow requires tags to match this
# version name exactly and requires the code to increase for every update.
homeStashTvVersionCode=1
homeStashTvVersionName=0.1.0-rc.1
